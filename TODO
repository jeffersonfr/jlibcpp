. gethostbyaddr is obsolescent, use getaddrinfo() instead.
. gethostbyname is obsolescent, use getnameinfo() instead.
. fix jmenu and jcombobox to new version of jlibcpp (use dialogs)
. fix dead-lock at jgui(x11)/tests/fullscreen.cpp (the repaint is lock the main loop)
. optimize the japplication::repaint() to avoid not needs locks
. implements the both flips in gtk3
